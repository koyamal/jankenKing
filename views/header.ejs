<header>
    <a href="/play">遊ぶ</a>
    <a href="/logout">ログアウト</a>

    <div>
        <% if(locals.userName === "Guest"){ %>
            <p><%= locals.userName %></p>
        <% } else { %>
            <p><%= locals.userName %></p>  
            <p><%= locals.point %> pt</p>         
        <% } %>         
    </div>
</header>